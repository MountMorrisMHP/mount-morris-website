name: Deploy to Production

# Production deploys ONLY when you press the button:
#   Actions tab -> "Deploy to Production" -> Run workflow
# It builds whatever is on the branch you pick (default: main), publishes it to
# the public site root, and VERSIONS the release with a git tag + GitHub Release.
# No PR, no approval, no change list.
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag (leave blank to auto-generate, e.g. v2026.06.11-1430)"
        required: false
        default: ""

permissions:
  contents: write        # push the built site to gh-pages AND create tags/releases

concurrency:
  group: deploy-production
  cancel-in-progress: false   # never interrupt a production publish mid-way

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production     # tracks deploy history + shows the live URL
      url: https://mountmorrismhp.github.io/mount-morris-website/
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history so tagging works

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # Use the typed version, or auto-generate one from the UTC date/time.
      - name: Determine version
        id: ver
        run: |
          V="${{ github.event.inputs.version }}"
          if [ -z "$V" ]; then V="v$(date -u +'%Y.%m.%d-%H%M')"; fi
          echo "version=$V" >> "$GITHUB_OUTPUT"
          echo "Releasing production version: $V"

      - name: Build static site (folders 1-3 only)
        run: node build.js
        env:
          VERSION: ${{ steps.ver.outputs.version }}

      - name: Publish to gh-pages root
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dist
          # keep_files preserves the staging/ and pr-preview/ subfolders.
          keep_files: true
          full_commit_message: "Deploy production ${{ steps.ver.outputs.version }} (${{ github.sha }})"

      # Record the exact source that went live as a tag + Release for rollback/history.
      - name: Tag and create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          V="${{ steps.ver.outputs.version }}"
          if git rev-parse "$V" >/dev/null 2>&1; then
            echo "Tag $V already exists — skipping tag/release creation."
            exit 0
          fi
          git tag -a "$V" -m "Production deploy $V"
          git push origin "$V"
          gh release create "$V" \
            --title "$V" \
            --notes "Production deploy from \`${{ github.ref_name }}\` @ ${{ github.sha }} on $(date -u '+%Y-%m-%d %H:%M UTC'). Live at https://mountmorrismhp.github.io/mount-morris-website/"
